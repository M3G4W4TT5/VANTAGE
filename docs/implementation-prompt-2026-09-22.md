Work in /home/megawatts/Projects/VANTAGE. Implement the first approved milestone in docs/implementation-plan-2026-09-22.md: steps 1–3 of that plan, covering the baseline, platform ownership/identity foundation, and first real Keycloak authentication integration. This is authorization to implement that milestone, including its necessary code, contracts, migrations, configuration, tests and operational documentation.

The preparatory documentation and assets are already committed and pushed. Inspect the actual checkout and preserve existing work. Do not repeat the preparatory documentation update, commit or push. The implementation plan and this prompt are the subsequent handoff; they may still be local files.

Read AGENTS.md, the implementation plan, PROTOTYPE_SPEC.md, DESIGN.md, VANTAGE_ECOSYSTEM.md and the decisions required by AGENTS.md. Use docs/atlas-workspace-change-record-2026-09-22.md for decision history. Inspect the current code and README commands before editing; approved requirements are not evidence that features already exist.

Act as the coordinating Astra Ultra agent. Carry steps 1–3 through implementation and verification in order, continuing between them without routine reapproval. Make ordinary implementation choices within the approved architecture. Use subagents for bounded, independent work where useful, such as dependency inspection, focused implementation, tests or review. Give each clear scope and file ownership, and review/integrate its result. Keep shared contracts, migration ordering, generated files and local services coordinated; do not distribute dependent steps to agents simultaneously.

Preserve existing aircraft/earthquake behaviour, shared components, stable source/observation identities, provenance, domain revision rules and saved work. Test migrations against isolated PostgreSQL/PostGIS and back up the operator's data before applying changes. Never recreate the existing data volume as a shortcut. Establish genuine platform ownership and app independence, plus explicit ownership assignment to the configured initial operator.

Deliver real Keycloak password + authenticator-app TOTP sign-in, backend OIDC code + PKCE, backend-managed sessions, protected REST/SignalR access and sign-out. Enforce ownership, CSRF protection and bounded expiry/revocation cleanup. Establish the session-cancellation contract for later recording; the full recording interface is a later feature. Keep credentials, tokens and enrollment material out of documents, logs, screenshots and exports. Verify the real provider flow separately from fixtures; explain any remaining manual enrollment or access blocker precisely.

Create or maintain docs/implementation-progress-2026-09-22.md with completed work, changed paths, checks and results, unresolved issues, current runtime state and the exact next action. Keep it usable by a fresh context. Run verification appropriate to each change, regenerate affected API clients, and update actual setup/migration/run commands. Finish by rebuilding the local application for review and reporting the milestone results and limitations.

Stop after this plan's step 3 for my review. Home is plan step 4; NEXUS, composed ATLAS, GeoJSON and broader connector work follow later. Steps 1–3 here refer to the implementation plan, not the differently numbered specification stages. Do not claim full prototype acceptance. Begin with the baseline inspection, then proceed with implementation.
