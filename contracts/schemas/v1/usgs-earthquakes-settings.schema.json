{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vantage.local/schemas/v1/usgs-earthquakes-settings.schema.json",
  "title": "USGS earthquake connection settings v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["pollSeconds"],
  "properties": { "pollSeconds": { "type": "integer", "minimum": 60, "maximum": 3600, "readOnly": true } }
}
